# 医疗多 Agent RAG 项目面试问答

## 0. 使用方式

本文按真实面试节奏组织：先要求候选人用 1 分钟说明项目，再逐层追问架构、数据、MySQL、检索、Agent、安全、评测与工程问题。回答以当前代码和验证结果为准，不把设计目标包装成已完成能力。

---

## 1. 项目总览

### Q1：请用 1 分钟介绍这个项目。

**参考回答：**

这是一个面向医疗信息辅助和问诊准备场景的多 Agent RAG 系统。核心流程由 LangGraph 编排，先进行输入安全检查和结构化问诊，再根据病例完整度决定追问或分诊；急症会提前进入安全响应，非急症才进入记忆读取、专家路由、MCP 联网检索、本地 Qdrant 向量检索、图谱扩展、证据融合、上下文预算、回答安全审查和随访。

项目当前真实落地了 LangGraph 条件路由、FastAPI、MySQL 8 知识治理库、Qdrant Local、`BAAI/bge-small-zh-v1.5`、PubMed/openFDA MCP、官方资料入库、证据动态评分和安全 fallback。Neo4j Server 与 TransH 在线推理仍是下一阶段，当前只实现了关系候选审核和 Neo4j CSV 导出。

### Q2：为什么选择医疗问诊场景，而不是普通知识问答？

**参考回答：**

医疗场景能暴露普通 RAG 容易忽略的问题：问题信息可能不完整，急症优先级高于回答完整度，证据存在时效和等级，药品禁忌需要监管说明书优先，多来源可能冲突，用户输入和检索内容都可能包含注入攻击。因此项目价值不是“接一个聊天模型”，而是构建可观测、可评测、可追溯的安全工作流。

### Q3：这个项目的边界是什么？

**参考回答：**

它不提供确定诊断，不直接开药或调整剂量，不替代医生。输出定位为症状整理、风险提示、就医准备、证据引用和随访提示。高风险输入优先建议及时就医。所有自动抽取的图谱关系默认是 `pending`，未经医学审核不能进入生产图谱。

### Q4：项目最有价值的三个工程点是什么？

**参考回答：**

1. LangGraph 条件路由将医疗安全决策编码成状态机，信息不足、急症和普通问诊走不同路径。
2. MySQL、Qdrant 和 Neo4j 导出职责分离：MySQL 管治理和审计，Qdrant 管语义召回，图谱管关系扩展。
3. 权威等级、临床证据强度和版权状态分离，并在证据融合中输出可解释的分项评分。

---

## 2. LangGraph 与 Agent 编排

### Q5：为什么说你的 LangGraph 不是简单线性流水线？

**参考回答：**

早期版本确实是把节点按顺序连接，急症也会继续检索。后来改为两个条件路由：`route_after_intake` 根据缺失槽位和语义红旗决定追问或分诊；`route_after_triage` 根据风险等级决定急症 early exit 或进入普通 RAG。这样图的控制流由 state 决定，而不是所有节点无条件执行。

### Q6：为什么信息不足时不直接检索？

**参考回答：**

“肚子疼怎么办”缺少部位、持续时间、严重程度和红旗症状，直接检索会产生大量泛化证据。系统先追问高信息增益槽位，既提高检索精度，也降低错误建议风险和无效 token 消耗。

### Q7：急症为什么直接 early exit，不先检索指南？

**参考回答：**

对于胸部压榨痛、出汗、呼吸困难这类组合，在线系统的首要任务是提示及时就医，而不是等待完整检索和生成。检索延迟和不确定性不能阻塞安全响应。后续可在不影响首屏提示的情况下异步补充证据，但当前 MVP 直接 early exit。

### Q8：Agent 是真的自主智能体吗？

**参考回答：**

当前更准确的说法是“职责化节点 + 条件路由”，不是完全自治 Agent。每个节点有独立职责和状态契约，但大部分逻辑仍是确定性规则或可替换组件。这样做便于测试和安全审计。后续可以把某些节点替换为结构化输出 LLM，但外层策略和权限边界仍应由代码控制。

### Q9：为什么不用 CrewAI 或完全自由的多 Agent 协作？本项目中心化调度架构有什么优势？

**参考回答：**

医疗问诊是容错率为零的高风险严肃场景，核心诉求是**确定性、可控性、安全闭环与极低延迟**。
去中心化的自由对话（如 CrewAI 角色扮演或 AutoGen 群聊）存在三大严重缺陷：
1. **链路黑盒不可控**：Agent 之间点对点随意调用极易产生死循环与意图偏离；
2. **信息自然语言失真**：中间通信用文字唠叨传递，严重衰减核心医学事实；
3. **缺乏严格 SLA 超时保障**：无法对单个 Agent 施加毫秒级超时熔断。

我们重构为 **中心化主控调度器 (`CentralOrchestrator`) + 模块化垂直 Agent** 架构：
- **主控唯一与零横向依赖**：所有垂直 Agent（用药安全、就医分诊、急症熔断、随访）互不通信，全由主控规划 DAG 并通过 `asyncio` 并行/串行调度；
- **强制 JSON Envelope 契约**：Agent 之间全部使用结构化 JSON 交互，最前端带唯一 `trace_id`，落盘至 SQLite `agent_trace_logs` 与 JSONL，便于排查重放；
- **独立 3.0s 超时与安全兜底**：子 Agent 独立超时熔断，超时自动降级，保障整体可用性；
- **中心安全审核**：由 `SafetyAuditAgent` 统一质检，违规支持 1 次纠偏重生成，失败则安全熔断。

---

## 3. MySQL 数据库迁移与设计

### Q10：为什么从 SQLite 迁移到 MySQL？

**参考回答：**

SQLite 适合单机原型，但项目已经包含采集任务、API 服务、人工审核、索引任务和后续图谱导出，存在并发写入、连接管理、远程部署和权限隔离需求。MySQL 8 提供 InnoDB 事务、行级锁、连接池生态、备份复制、账户权限和更好的服务化扩展能力，更符合后端项目定位。

### Q11：MySQL 中有哪些核心表？

**参考回答：**

核心表包括：

- `sources`：来源机构、权威等级、版权策略。
- `documents`：文档版本、证据等级、全文、hash 和审核状态。
- `chunks`：页码、章节、chunk 类型和文本。
- `guideline_profiles`：诊断、分层、随访、转诊和红旗字段。
- `drug_monographs`：药品说明书结构化字段。
- `textbook_profiles`：教材版本和授权范围。
- `entities`、`document_entities`：实体标准化和文档链接。
- `relation_candidates`：带证据和审核状态的关系候选。
- `crawl_runs`：采集审计。

### Q12：如何保证中文、特殊符号和医学术语正确存储？

**参考回答：**

数据库和表使用 `utf8mb4` 与 `utf8mb4_0900_ai_ci`；连接也指定 `utf8mb4`。JSON 字段用于别名、学科和错误列表，长正文使用 `LONGTEXT`。业务主键之外还保存 SHA-256，用于内容去重和版本检测。

### Q13：MySQL upsert 如何实现？

**参考回答：**

使用 `INSERT ... ON DUPLICATE KEY UPDATE`。来源以 `slug` 唯一，文档以 `(source_id, external_id)` 唯一，实体以 `(canonical_name, entity_type)` 唯一。使用 `LAST_INSERT_ID(primary_key)` 技巧，使插入和更新路径都能返回稳定主键。

### Q14：为什么关系表增加 `relation_key`？

**参考回答：**

MySQL 唯一索引中 `NULL` 可以出现多次，直接把可空 `chunk_id` 放进复合唯一键会产生重复。项目对主体、谓词、客体、文档和 chunk 计算 SHA-256 `relation_key`，用固定非空键保证幂等。

### Q15：`--reset` 有什么风险？

**参考回答：**

`--reset` 会关闭外键检查并删除、重建项目的 10 张业务表，但不会执行 `DROP DATABASE`。它仍只适用于开发和可重放数据集。生产环境不能开放这个参数，应使用迁移工具、备份、最小权限账户和分环境配置。

### Q16：MySQL 迁移目前验证到什么程度？

**参考回答：**

代码、PyMySQL 配置解析、DDL、语法和上层调用已经完成迁移，也提供 MySQL 8 Compose。当前执行环境没有 Docker 和 MySQL 客户端，因此不能声称在这台机器完成了真实 MySQL 服务端建表回归。面试时应诚实说明：代码级迁移完成，真实服务端 E2E 需要启动 Compose 后重新跑入库、索引和导出命令。

### Q17：MySQL 如何做生产优化？

**参考回答：**

会引入连接池、超时、重试边界、只读/写账户分离、慢查询日志和迁移工具；对高频查询建立 `(authority_tier, evidence_grade)`、`(rights_status, ingestion_mode)`、关系审核状态等索引。大规模全文不应长期全部存 MySQL，可把原始文件放对象存储，MySQL 保存元数据和可检索文本块。

---

## 4. 数据治理与版权

### Q18：为什么不能直接爬取医学教材全文？

**参考回答：**

教材和商业工具书通常受著作权保护，购买阅读权限不等于获得批量抓取、索引、模型训练和再分发权。项目默认只允许 `metadata_only`；全文需要 `public_domain`、`open_license`、`licensed` 或 `user_provided_authorized`，教材还必须提供 `license_proof_path`。

### Q19：如何区分权威来源与高质量证据？

**参考回答：**

`authority_tier` 表示发布机构权威，`evidence_grade` 表示推荐背后的研究确定性。全国医学会专家共识可能是 `A2` 权威来源，但证据质量仍可能是低或未分级。两者不能合并成一个“A级”。

### Q20：如何处理指南版本更新？

**参考回答：**

文档保存 `version`、`published_at`、`effective_from`、`valid_until`、内容 hash 和来源 URL。新版本入库后先跑回归检索和安全指标，再切换默认版本；旧版本保留审计但降低或取消在线权重。生产中还应增加“被替代/撤回”状态。

### Q21：自动抽取的图谱关系能直接上线吗？

**参考回答：**

不能。规则抽取关系为 `pending`，必须保留 `document_id`、`chunk_id`、证据原句、置信度和抽取方法。只有医学人工审核后的 `approved` 关系才导出到生产 Neo4j。高风险药物关系应双人复核。

---

## 4.5 记忆系统与多就诊人实体隔离

### Q21_1：医疗场景下，如何保证多用户或同一用户切换就诊人时记忆系统的干净？

**参考回答：**

医疗问诊绝不能简单将“登录账号”当做唯一的记忆上下文。在真实场景中，往往是一个账号为多名家庭成员（如自己、父母、孩子）代问诊。如果只按 `user_id` 隔离，孩子的急性支气管哮喘与母亲的高血压、青霉素过敏就会严重交叉感染，造成误诊或致命用药冲突。

我们设计并落地了三重安全净化机制：
1. **三级实体模型 (User -> Patient Profile -> Consultation Session)**：
   - 以 `(user_id, patient_id)` 复合键作为存储与检索的绝对物理分区键。
   - 在 `MedicalMemoryStore` 中建立 `patient_profiles`、`consultation_episodes`、`session_registry` 和 `memory_audit_logs` 四张表，实现 ACID 强隔离。
2. **会话防串户注册校验与碰撞重绑**：
   - 会话首次创建即与 `(user_id, patient_id)` 锁定。若客户端或网络竞态导致同一 `session_id` 被切换后的另一个就诊人复用，系统自动拦截会话碰撞（`session_tenant_mismatch`）并安全重绑至当前就诊人，防止多 Tab 或会话重放攻击。
3. **零信任记忆写入守卫 (`MemoryWriteGuard`) 与切换硬净化**：
   - 运行时图状态中携带 `isolation_key` 签名，末端写入前比对防伪凭据，任何跨实体上下文污染痕迹直接拦截拒写并告警。
   - 提供 `/patients/switch` 原子切换接口与 `/memory/purge` 物理级硬删除接口，切换时前端重置状态与 Session，后端仅回传纯净画像与历史摘要，确保记忆系统零残留。

---

## 5. RAG、Qdrant 与 Embedding

### Q22：为什么同时使用 MySQL 和 Qdrant？

**参考回答：**

MySQL 是事实来源和治理库，适合事务、版本、版权、审核和结构化查询；Qdrant 负责高维向量近邻检索。MySQL 不能替代向量索引，Qdrant 也不适合承担复杂治理事务。两者通过 `document_id/chunk_id` 关联。

### Q23：为什么选择 `BAAI/bge-small-zh-v1.5`？

**参考回答：**

它体积和资源成本适合本地 CPU MVP，对中文检索有较好基线，FastEmbed ONNX 部署简单，维度为 512。它不是医学专用模型，所以项目保留医学词典、实体字段和后续医学 embedding/reranker 对比路线。

### Q24：为什么不直接使用更大的 embedding？

**参考回答：**

项目优先验证完整闭环和可运行性。更大的模型会增加下载、内存和索引成本，但未必在当前 1177 chunks 上显著提升。正确做法是构建标注查询集，用 Recall@k、MRR、nDCG 和延迟做对比后再升级。

### Q25：Qdrant 索引曾出现过什么问题？

**参考回答：**

早期 `index_documents()` 把每次传入 JSON 当成整个 collection 快照，运行 demo 会把正式 1177 个知识点覆盖成 12 个。修复后引入命名空间：`mysql_database` 是正式库，`runtime_knowledge` 是无正式库时 fallback，`knowledge_file_<hash>` 是独立文件索引。问诊检测到正式库后只检索 `mysql_database`。

### Q26：混合检索目前真的有 BM25 吗？

**参考回答：**

当前真实主链路是 Qdrant 向量检索、实体词典归一化、MCP 联网证据和 mock/候选图谱扩展；严格意义上的生产级 BM25 索引尚未完整落地。文档中的 BM25 是目标架构，面试时不能说成已完成。下一步可接 Elasticsearch/OpenSearch 或实现本地稀疏检索，并做 RRF 融合。

### Q27：为什么检索结果还需要证据融合？

**参考回答：**

向量相似度只表示语义接近，不代表权威、最新或适用于当前人群。融合分数考虑来源类型、权威等级、证据等级、相关性、可信度、时效性和问题类型。药品禁忌问题会提高监管说明书权重，诊断和转诊问题会提高指南权重。

### Q27_1：知识图谱增强中为什么选择 TransH 而不是 TransE？BGE 热启动是怎么实现的？

**参考回答：**

在医学三元组中，存在大量极其严苛的 1-to-N 关系（例如单一疾病对应多个红旗表现，单一药物对应多种不良反应与禁忌）。TransE 只有一个全局向量空间，如果让 $h + r \approx t_1$ 且 $h + r \approx t_2$，在代数上必然逼迫 $t_1 \approx t_2$，造成红旗症状与良性症状的几何混淆崩溃。

TransH 通过为每个关系学习一个超平面法向量 $w_r$ 和平移向量 $d_r$（施加 $\|w_r\|_2 = 1.0$ 与 $w_r^\top d_r = 0$ 约束），将头尾实体投影至关系特异性超平面 $h_\perp = h - (w_r^\top h) w_r$，彻底解耦了实体在不同关系下的语义表达。

我们的热启动（Warm-start）方案是：
1. **实体向量初始化**：实体不再随机初始化，而是直接利用已有的 `BAAI/bge-small-zh-v1.5` 生成 512 维稠密向量并作单位球面归一化，零样本继承了海量中文医学词汇的语义分布。
2. **关系正交微调**：保持实体向量的主干特征，仅微调关系的法向量 $w_r$ 与位移向量 $d_r$，通过 Margin Ranking 损失函数迭代收敛，既保证了极快的收敛速度，又避免了小样本图谱上的灾难性遗忘。
3. **几何能量打分**：线上推理时直接计算欧氏距离能量 $f_r(h, t) = \|h_\perp + d_r - t_\perp\|_2$ 并经 Sigmoid 映射为置信度，用于剪枝过滤弱关联和辅助证据融合排序。

---

## 6. MCP 与联网工具

### Q28：MCP 在项目中的作用是什么？

**参考回答：**

MCP 把 PubMed 和 openFDA 等外部工具隔离成标准工具接口，LangGraph 节点通过 client 调用。好处是工具协议、参数和返回结构可统一，未来可以替换或扩展服务，而不改变主图。

### Q29：联网工具失败怎么办？

**参考回答：**

网络失败只记录 `tool_events`，主流程回退本地 Qdrant，不让整个问诊失败。生产中还应增加超时、有限重试、熔断、缓存和工具健康检查。

### Q30：如何防止工具滥用？

**参考回答：**

工具名必须在白名单，参数需要 schema 校验，限制 top-k、超时和调用预算；用户原始隐私字段不能直接拼进外部查询；工具返回被视为不可信数据，必须经过 RetrievedContentGuard 和证据验证。

---

## 7. 安全与医疗风险

### Q31：语义安全识别是不是一个真正的语义模型？

**参考回答：**

当前是本地 deterministic 语义层：将表达变体映射为 prompt injection、角色劫持、危险用药请求和红旗症状 signals，并输出置信度和证据片段。它比固定单关键词更完整，但不是训练好的分类模型。后续可以在保持输出 schema 不变的情况下替换为 embedding 分类器或结构化 LLM judge。

### Q32：如何处理“没有便血”这样的否定表达？

**参考回答：**

安全层增加了邻近否定过滤，识别“没有、无、未、不、否认”等前缀，避免把“没有便血”当成红旗。这个修复是在真实 sample case 中发现的。

### Q33：如何防止检索内容中的 prompt injection？

**参考回答：**

检索内容默认不可信。`WebEvidenceCleaner` 会识别“忽略之前指令、system prompt、不要遵守”等注入信号；低可信网页、偏方、根治和“不用就医”等内容会被丢弃。生产中应进一步做结构化隔离和模型侧引用约束。

### Q34：回答层如何防止危险用药建议？

**参考回答：**

专家节点只输出用药安全提示，不给具体处方；`OutputSafetyGuard` 检测自行加量、保证根治、不用就医等危险表述。药品问题证据排序优先监管说明书，冲突无法消解时建议医生或药师确认。

---

## 8. 评测与可观测性

### Q35：你如何评价系统好坏？

**参考回答：**

分四层：

1. 检索：Recall@k、Precision@k、MRR、nDCG、权威来源比例。
2. 回答：faithfulness、answer relevance、citation accuracy、unsupported claim rate。
3. 安全：红旗召回、禁忌漏检、危险建议率、攻击通过率。
4. 工程：p50/p95 延迟、超时率、fallback 率、缓存命中和成本。

### Q36：为什么不能只用 LLM-as-judge？

**参考回答：**

LLM judge 有偏差、漂移和成本问题。项目保留确定性指标和 critical fail，例如漏掉急症、给出危险剂量、引用不存在来源应直接失败。LLM judge 适合补充语义质量，不应成为唯一裁判。

### Q37：你有哪些真实验证结果？

**参考回答：**

当前官方库包含 15 份文档、1177 chunks、44 个实体和 236 条关系候选；11 条人工批准关系可导出 Neo4j。Qdrant 正式命名空间有 1177 点，使用 512 维 BGE-small-zh。正常腹痛 case 输出中风险并融合 3 条证据；攻击 case 能进入急症 early exit；信息不足 case 只追问、不检索。

### Q38：如何追踪一次回答为什么生成？

**参考回答：**

LangGraph state 保存每个节点 trace，证据保存来源、检索路径、相关性和 `fusion_score_breakdown`，图谱边保存证据原文和 chunk。最终报告包含风险、专家路由、工具事件、向量状态、证据、token 预算、安全审查和评测摘要。

### Q38_1：大模型不是支持 1M 超长上下文吗？为什么还要做冷热分区和 Token 预算控制？

**参考回答：**

1. **破除“上下文越大越好”的误区**：在严肃医疗问答中，将大量长指南或无关轮次直接塞入超长上下文，会引发严重的“中间迷失 (Lost in the Middle)”注意力稀释问题，不仅大幅推高首字延迟（TTFT）与计算成本，还会增加虚构用药与幻觉风险。
2. **纯动态比例驱动模型 (`TokenBudget`)**：系统不强行绑死在某个固定数字上，而是确立黄金几何切分比例：
   - 知识检索证据占 **55%**（以完整的 Clinical Chunks 结构体组织，拒绝单句断章取义）；
   - 历史对话与状态卡片占 **35%**；
   - 系统指令与安全底线预留占 **10%**。
   调用方可根据所用模型与场景自由传入 `total`，系统自动按比例防御边界。
3. **冷热分区与 70%/85% 双水位线**：
   - 最近三轮是热区，始终完整保留，不参与摘要，也不被召回结果替换；
   - 历史消耗达到历史预算的 70% 后，只对三轮之前的冷区执行医学 NLP 摘要合并，抽取：
     - `active_symptoms`：阳性症状（如：右下腹痛、频繁恶心）；
     - `ruled_out_negatives`：明确排除的医学阴性体征（如：否认便血、无发热，杜绝病史遗漏）；
     - `medication_trials`：已尝试过的药物（如：布洛芬）；
     - `timeline_clues`：病程发展时间线。
   摘要进入 `session_state_card`，保留冷区来源与阈值元数据。若压缩后仍超过历史预算的 85%，冷区原文用 BGE 向量化写入租户隔离的 `cold_dialogue_vectors`；后续按当前问题 Top-K 召回，且召回有独立小预算，不能挤掉热区。

---

## 9. 系统设计与扩展

### Q39：如果数据量扩大到百万 chunks，怎么改？

**参考回答：**

MySQL 保存元数据、版本和审核，不把大原始文件全部塞数据库；原文件迁移对象存储。Qdrant Local 改为 Qdrant Server 集群，按 collection/tenant 做隔离，批量异步 embedding，增量 upsert。检索增加科室过滤、稀疏+稠密融合、reranker、缓存和离线索引发布流程。

### Q40：如何保证多实例 API 不冲突？

**参考回答：**

当前 Qdrant Local 有文件锁，只适合单 worker。生产改独立 Qdrant Server。MySQL 使用连接池和事务；采集任务、索引任务与在线 API 分离，通过版本号发布新索引。在线服务只读已发布版本。

### Q41：如果 MySQL 和 Qdrant 数据不一致怎么办？

**参考回答：**

MySQL 是 source of truth。索引点保存 `document_id`、`chunk_id`、内容 hash 和命名空间。重建流程从 MySQL 生成 Qdrant 全量快照；增量流程按 hash upsert 并删除失效点。发布前比较 MySQL 可索引 chunk 数与 Qdrant namespace count，并跑抽样回查。

### Q42：为什么还需要 Neo4j？

**参考回答：**

Qdrant擅长语义相似，Neo4j适合明确关系和多跳约束，例如药物—禁忌人群—疾病、疾病—红旗—科室。图谱可以做关系扩展和可解释路径，但不能把自动候选直接视为事实。当前已实现审核 staging 和 CSV，尚未完成在线 Neo4j Server 查询。

### Q43：如果重新做一次，你会先改什么？

**参考回答：**

我会更早建立数据契约和测试，而不是先写大而全的设计文档。优先补：MySQL 真实服务端集成测试、BM25/RRF、检索标注集、关系审核界面、条件图单测和生产化 Qdrant Server。这样能减少“文档能力大于代码能力”的问题。

---

## 10. 压力追问与诚实回答

### Q44：你的项目是不是堆技术名词？

**参考回答：**

早期确实存在设计超前于实现的问题，所以后来按“真实落地/目标设计”重新区分。目前真实落地的是 LangGraph 条件流、FastAPI、MySQL 数据层代码、Qdrant+BGE、MCP、官方数据入库、版权治理和证据排序；Neo4j Server、TransH、正式 LTP、生产 BM25 和 Web Console 仍未完成。面试时只对已验证能力负责。

### Q45：为什么 MySQL 迁移后还不能说完全完成？

**参考回答：**

迁移完成指代码和配置已切换；但当前机器没有 Docker/MySQL 服务，因此没有完成真实 MySQL 8 的 E2E 建表与数据回灌。要闭环必须启动 Compose，执行官方数据入库、Qdrant 重建、Neo4j 导出和 API smoke test。不能把静态检查说成服务端验证。

### Q46：医疗项目最严重的风险是什么？

**参考回答：**

不是模型“答得不够漂亮”，而是漏掉急症、给出危险用药建议、引用过期或不适用证据，以及把未经审核的关系当事实。因此项目采用急症 early exit、监管证据优先、来源版本审计、关系审核状态和 fail-closed 策略。

### Q47：这个项目如何体现你的个人贡献？

**参考回答：**

可以重点讲三个真实改造：

1. 将线性 LangGraph 改成追问/急症/普通 RAG 条件流。
2. 构建官方资料到 MySQL、Qdrant、Neo4j 导出的治理链路，并加入版权闸门。
3. 发现并修复 Qdrant demo 覆盖正式 1177 点索引的问题，引入正式库优先的命名空间隔离。

这些都可以用代码、运行报告和 before/after 结果证明。

---

## 11. 候选人反问面试官

1. 团队当前更关注 RAG 召回质量、Agent 工作流，还是推理服务稳定性？
2. 线上知识库是否有文档版本、权限、删除和审计要求？
3. 团队如何构建检索标注集，是否有真实业务反馈闭环？
4. LLM-as-judge 在团队中如何校准，是否与人工评测做一致性分析？
5. 向量数据库和关系数据库的职责如何划分，是否有增量索引发布机制？
6. 对实习生而言，入职前三个月最希望交付的工程结果是什么？
